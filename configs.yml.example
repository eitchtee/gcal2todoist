todoist_api_token: "YOUR_TODOIST_TOKEN"
default_project: "Events"
label: "Event"

# If false, script will run only once. Else it will run every n seconds
keep_running: false
run_every: 600 # seconds

log_level: "INFO"

task_prefix: "* 🗓️ ```"
task_suffix: "```"

# Any event with this label set by you will be completed and not added again
completed_label: "Done"

# Will fetch this number of days in advance. The higher this number
# the higher the amount of api calls, watch out for rate limiting
days_to_fetch: 7